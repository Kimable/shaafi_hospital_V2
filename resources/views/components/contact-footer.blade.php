<div class="contact-section container py-5">
    <p class="subtitle text-center text-uppercase fw-bold">
        {{ __("Get in touch") }}
    </p>
    <h2 class="fw-bolder text-center pb-3">{{ __("Contact Us") }}</h2>
    <div class="row">
        <div class="col-lg-3 col-sm-6">
            <div class="card text-center py-5 px-2">
                <i class="bi bi-telephone-inbound"></i>
                <h5 class="subtitle fw-bold">{{ __("Emergency") }}</h5>
                <p class="">+252 61 2877778</p>
            </div>
        </div>

        <div class="col-lg-3 col-sm-6">
            <div class="card text-center py-5 px-2">
                <i class="bi bi-geo-alt"></i>
                <h5 class="subtitle fw-bold">{{ __("Location") }}</h5>
                <p>{{ __("Dagmada Hodon Mogadishu Banaadir, Somalia") }}</p>
            </div>
        </div>

        <div class="col-lg-3 col-sm-6">
            <div class="card text-center py-5 px-2">
                <i class="bi bi-envelope-at"></i>
                <h5 class="subtitle fw-bold">{{ __("Email Us") }}</h5>
                <p>info@shaafihospital.so</p>
            </div>
        </div>

        <div class="col-lg-3 col-sm-6">
            <div class="card text-center py-5 px-2">
                <i class="bi bi-alarm"></i>
                <h5 class="subtitle fw-bold">{{ __("Working Hours") }}</h5>
                <p>{{ __("We are always open, 24/7") }}</p>
            </div>
        </div>
    </div>
</div>
<style>
    .contact-section .card,
    .contact-section .card .subtitle {
        background-color: var(--tertiary);
        color: var(--light);
    }
</style>
